version: "3"
services:
    webapp:
        build: .
        command: carton exec -- morbo myapp.pl
    nginx:
        image: nginx:mainline-alpine
        volumes:
            - ./nginx.conf:/etc/nginx/nginx.conf:ro
        ports:
            - "8080:80"
